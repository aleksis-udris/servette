[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aleksis-udris", email = "aleksis060806@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
api = "api:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
